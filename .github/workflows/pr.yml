name: PR

on:
  pull_request:

permissions:
  actions: read
  contents: read
  pull-requests: read

jobs:
  ci:
    # Pin: #12858 merge — docker context integrity gate + traceability context fix.
    uses: paperclipai/paperclip/.github/workflows/pr-trusted.yml@539c9212f4b98e37643f5a8e3b603f1b5845b5d7
